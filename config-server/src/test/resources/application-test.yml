spring:
  application:
    name: config-server-test
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

server:
  port: 0

logging:
  level:
    root: INFO
